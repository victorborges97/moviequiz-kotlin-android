<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/clCardSearch"
    android:layout_width="wrap_content"
    android:layout_margin="16dp"
    android:paddingHorizontal="30dp"
    android:paddingVertical="20dp"
    android:foreground="@drawable/ripple"
    android:clickable="true"
    android:background="@drawable/shap_radius_20dp_card_search"
    android:layout_height="wrap_content"
    android:focusable="true">

    <ImageView
        android:id="@+id/ivPhotoMovieSearch"
        android:layout_width="80dp"
        android:layout_height="120dp"
        app:layout_constraintBottom_toTopOf="@+id/tvTitleSearch"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginBottom="10dp"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvTitleSearch"
        android:layout_width="0dp"
        android:maxLines="2"
        android:textAlignment="center"
        android:ellipsize="marquee"
        android:textColor="@color/white"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="American Pie 2"/>

</androidx.constraintlayout.widget.ConstraintLayout>