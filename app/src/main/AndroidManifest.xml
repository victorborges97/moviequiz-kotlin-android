<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.borges.moviequiz">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieQuiz">
        <activity
            android:name="br.borges.moviequiz.ui.about.AboutActivity"
            android:parentActivityName="br.borges.moviequiz.ui.main.MainActivity"
            android:exported="true" />
        <activity
            android:name="br.borges.moviequiz.ui.search.SearchMovieActivity"
            android:exported="true" />
        <activity
            android:name="br.borges.moviequiz.ui.create.CreatePostActivity"
            android:exported="true" />
        <activity
            android:name="br.borges.moviequiz.ui.register.RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name="br.borges.moviequiz.ui.main.MainActivity"
            android:exported="true" />
        <activity
            android:name="br.borges.moviequiz.ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>